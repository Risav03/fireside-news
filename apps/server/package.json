{
  "name": "@repo/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/core": "*",
    "@repo/db": "*",
    "@repo/queue": "*",
    "@repo/utils": "*",
    "bullmq": "^5.76.4",
    "dotenv": "^17.4.2",
    "tsx": "^4.21.0"
  }
}
